<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FindaCook.SearchPage"
             Title="SearchPage">
    <StackLayout>
        <!-- Search Bar -->
        <SearchBar Placeholder="Search items..." PlaceholderColor="BlueViolet" CancelButtonColor ="Black"/>

        <!-- Search History (ListView) -->
        <!--<ListView
            x:Name="searchHistoryListView"
            ItemsSource="{Binding SearchHistory}"
            IsVisible="{Binding IsSearchHistoryVisible}"
            HasUnevenRows="True">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout>
                            <Label Text="{Binding .}" />
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>-->
    </StackLayout>
</ContentPage>